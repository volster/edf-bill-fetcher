# Task 13 (6f.2): HTML dispatcher parity + CLI/GUI wiring — evidence

Date: 2026-08-24 (task wave dated 2026-08-06)
Commit: feat(html): enforce 3-format dispatcher parity, wire CLI + GUI

## What changed
- tests/test_dispatch_parity.py: HTML dispatcher parity tests
  (registry coverage, three-format lockstep, all-callable), a real
  end-to-end CLI `--html-report` smoke test, and a GUI Output Format
  presence test (asserts the "html" radio value exists in
  ReportOptionsDialog).
- edf_bill_fetcher/io/cli.py: `run_cli_html_report` (arg + handler,
  mirroring the PDF/DOCX entry points) and `main()` dispatch for
  `--html-report`; no-tkinter hint updated.
- edf_bill_fetcher/ui/app.py: `HAS_HTML_REPORT` detection, "HTML Only"
  radio in the Output Format frame, and HTML threaded through both
  `_run_auto_report` and `load_spreadsheet_and_report`.
- tests/test_io_cli_argv.py: `TestRunCliHtmlReport` branch coverage
  (bare list, wrapper unwrap, failure exit 1, config load,
  engine-data pickle, exception handler) + `main()` dispatch test.
- tests/test_ui_app_dialog_handlers.py: new html-only / both-includes-html
  auto-report tests and an html-format load&report test; existing
  pdf/docx-only scenarios pinned `HAS_HTML_REPORT=False`.

## RED (watched the new tests fail before implementing)
- tests/test_dispatch_parity.py:
  FAILED TestCliHtmlReportSmoke::test_cli_html_report_smoke_produces_file
    ImportError: cannot import name 'run_cli_html_report'
  FAILED TestGuiHtmlFormatPresence::test_gui_report_options_dialog_exposes_html_format
    AssertionError: assert 'html' in ['both', 'pdf', 'docx']
  (the 4 registry-parity tests pass immediately: the merged renderer's
  literal section_builders already satisfies the invariant — the tests
  pin it as a lockstep regression gate.)
- tests/test_io_cli_argv.py: ERROR during collection (missing
  run_cli_html_report).
- tests/test_ui_app_dialog_handlers.py: 7 failed (missing
  HAS_HTML_REPORT / html behavior).

## GREEN
- python -m pytest tests/test_dispatch_parity.py -q
  -> 25 passed (23 pre-existing + 2 new; 4 html parity tests included)
- python -m pytest tests/test_dispatch_parity.py tests/test_io_cli_argv.py tests/test_ui_app_dialog_handlers.py -q
  -> 139 passed
- Full suite: python -m pytest -q
  -> 1535 passed, 9 skipped, 33 warnings in 77.06s

## Lint / format / types
- ruff check . -> All checks passed!
- ruff format --check . -> 207 files already formatted
  (after `ruff format` on the 5 changed files)
- mypy . --exclude scratch/ -> Success: no issues found in 202 source files

## End-to-end CLI smoke (real main.py, conda env edf-bill-fetcher)
$ python main.py --html-report -i /tmp/opencode/task13-records.json -o /tmp/opencode/task13-report.html
Professional HTML report generated:
/tmp/opencode/task13-report.html
exit=0
-rw-rw-r-- 1 matthew matthew 16845 Aug 24 01:00 /tmp/opencode/task13-report.html
Section <h2 class="section"> headings in output: 15

## GUI format checkbox
The parity-file GUI test builds the real ReportOptionsDialog under the
test display and asserts the Output Format frame radiobuttons include
value "html" — passes. (No display problems: the suite ran green in
this environment without explicit xvfb-run.)

## QA scenario (failure mode)
Registry gains a key without an HTML builder -> the literal-dict AST
walk reports a key-set mismatch and test_html_dispatcher_covers_registry
/ test_pdf_docx_and_html_dispatchers_agree fail at CI time, before any
customer hits the runtime RuntimeError.
